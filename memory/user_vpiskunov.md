---
name: user preferences
description: Coding style and collaboration preferences for vpiskunov
type: user
---

- Prefers YAML block sequence format (`- item`) over JSON-style inline arrays (`[item1, item2]`) in config files
- Prefers concise, direct communication — no preambles, no summaries at end of responses
- Designs APIs iteratively: willing to change field types mid-implementation (e.g. related: map → []ResourceSelector)
- Wants examples to be fully functional and match real-world usage patterns
- Expects generated files in example charts to be committed (values.schema.json, _settings.tpl, etc.)
